machine: 
  environment: 
    CIRCLE_REVERSE_DEPENDENCIES: oauthd-service

general:
  branches:
    only:
      - patched

test: 
  override:
    - /bin/bash -c "cat package.json"

deployment: 
  prod: 
    branch: patched
    commands: 
      - "./circle_ci_trigger_build"
